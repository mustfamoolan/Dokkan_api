# إعداد الدومين (salesflowi.com) على سيرفر VPS

لربط الدومين بتطبيقك الذي يعمل على Docker (المنفذ 8000)، سنقوم بخطوات احترافية باستخدام **Nginx** كـ (Reverse Proxy).

## 1️⃣ إعداد DNS (في لوحة تحكم الدومين)
يجب توجيه الدومين إلى عنوان IP الخاص بالسيرفر.

1. اذهب إلى لوحة تحكم الدومين (سواء في Hostinger أو غيرها).
2. اختر **DNS / Zone Editor**.
3. أضف السجلات التالية:

| Type | Name | Content / Target | TTL |
|------|------|------------------|-----|
| **A** | **@** | `76.13.0.61` | 3600 |
| **A** | **www** | `76.13.0.61` | 3600 |

> ⚠️ **ملاحظة:** قد يستغرق التفعيل من دقيقة إلى 24 ساعة (غالباً دقائق).

---

## 2️⃣ تثبيت وإعداد Nginx (على السيرفر)
سنستخدم Nginx لاستقبال طلبات الدومين (منفذ 80/443) وتحويلها إلى تطبيقك (منفذ 8000).

**في التيرمنال (SSH)، نفذ الأوامر التالية:**

```bash
# 1. تحديث النظام وتثبيت Nginx
sudo apt update
sudo apt install -y nginx certbot python3-certbot-nginx

# 2. إنشاء ملف إعدادات للموقع
sudo nano /etc/nginx/sites-available/salesflowi.com
```

**انسخ والصق المحتوى التالي داخل الملف:**

```nginx
server {
    server_name salesflowi.com www.salesflowi.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**احفظ الملف واخرج:** (`Ctrl+O` ثم `Enter` ثم `Ctrl+X`).

**تفعيل الإعدادات:**

```bash
# إنشاء رابط للملف في sites-enabled
sudo ln -s /etc/nginx/sites-available/salesflowi.com /etc/nginx/sites-enabled/

# 🛠️ خطوة مهمة: حذف إعدادات Default إذا كانت تتعارض
sudo rm /etc/nginx/sites-enabled/default

# اختبار صحة الإعدادات
sudo nginx -t
# يجب أن تظهر رسالة: syntax is ok ... test is successful

# إعادة تشغيل Nginx
sudo systemctl restart nginx
```

✅ **الآن:** عند زيارة `http://salesflowi.com` سيفتح تطبيقك!

---

## 3️⃣ تأمين الموقع بشهادة SSL (HTTPS)
سنستخدم Certbot للحصول على شهادة مجانية وتجدد تلقائياً من Let's Encrypt.

```bash
sudo certbot --nginx -d salesflowi.com -d www.salesflowi.com
```

*   سيطلب منك بريد إلكتروني (ضع بريدك).
*   وافق على الشروط (`Y`).
*   سيسألك هل ترغب بإعادة توجيه HTTP إلى HTTPS؟ اختر **2 (Redirect)**.

---

## 4️⃣ تحديث إعدادات التطبيق
لضمان عمل الروابط والصور بشكل صحيح مع الدومين الجديد:

1. ادخل لمجلد المشروع:
   ```bash
   cd /docker/dokkan_api
   ```
2. عدّل ملف `.env` الفعلي:
   ```bash
   nano .env
   ```
3. عدّل القيم التالية:
   ```env
   APP_URL=https://salesflowi.com
   ASSET_URL=https://salesflowi.com
   ```
4. احفظ الملف، ثم نظف الكاش داخل الحاوية:
   ```bash
   docker compose exec app php artisan optimize:clear
   ```

---

🎉 **مبارك!** موقعك يعمل الآن على `https://salesflowi.com` بشكل آمن.
